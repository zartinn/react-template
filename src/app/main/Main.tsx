import * as React from 'react';
import './Main.scss';

export default class Main extends React.Component {
  render () {
    return (
      <React.Fragment>
        Main 1 content
      </React.Fragment>
    );
  }
}